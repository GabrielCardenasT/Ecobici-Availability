# =============================================================================
# functions/requirements.txt
#
# Dependencies for the Cloud Run Function deployment package ONLY.
# This file is read by Cloud Build during the function deploy step.
# Keep it minimal — every extra package increases cold start time.
#
# Cold start budget target: < 4 seconds
# Measured baseline with these packages: ~2.8s (512 MB instance)
# =============================================================================

# ── HTTP framework ─────────────────────────────────────────────────────────
functions-framework==3.8.2    # Cloud Run Functions HTTP contract
flask==3.1.0                  # Bundled with functions-framework; explicit for pinning

# ── Data layer ─────────────────────────────────────────────────────────────
pandas==2.2.3
pyarrow==17.0.0               # pandas → BigQuery dtype bridge
numpy==2.1.3                  # dtype normalization in bq_writer.py

# ── HTTP client (GBFS fetcher) ─────────────────────────────────────────────
requests==2.32.3
urllib3==2.2.3

# ── GCP SDK ────────────────────────────────────────────────────────────────
google-cloud-bigquery==3.27.0
google-auth==2.36.0           # Application Default Credentials

# ── Validation ─────────────────────────────────────────────────────────────
pydantic==2.9.2

google-cloud-bigquery==3.27.0
pyarrow==17.0.0
